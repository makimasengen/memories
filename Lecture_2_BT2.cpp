#include <iostream>
using namespace std;

main()
{
    double a,b;
    cin >> a >> b;
    if (a * b > 0)
    {
        cout << "Hai so cung dau";
    }
    else
    {
        cout << "Hai so khac dau";
    }
}
